/// Default template used by `flow create` when `--template` is omitted.
const String kDefaultTemplateUrl = 'https://github.com/loqmanali/flutter_starter.git';

/// Default git ref used by `flow create` when `--ref` is omitted.
const String kDefaultTemplateRef = 'main';
